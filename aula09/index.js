const nome = 'Neymmar';         // string
const nome2 = "Neymmar";        // string
const nome3 = `Neymmar`;        // string
const num = 10;                 // number
const num2 = 10.50;             // number
let nomeAluno;                  // undefined -> não aponta para local nenhum da memória
const sobrenomeAluno = null;    // nulo -> não aponta para local nenhum da memória
const aprovado = false;         // boolean = true or false (lógico)

console.log(typeof aprovado);   //Exemplo de como saber o tipo de uma variavel